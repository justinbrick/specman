# TODO

- fix(specman-mcp): inputSchema for update_artifact not working. -> create more tools to replace update_artifact (or if easier, use externally tagged schema?)
  - fix(specman-mcp): use external tagged schema for properties. DONE
- ref(specman-mcp): check-list tasks, staged tasks
- rev(specman-mcp): require revision impact analysis
- feat(specman-mcp): revision impact analysis (read dependencies, create scratches for fixing)
- feat(specman-mcp): MCP published (docker?)
- feat(specman-library): read constraint content given identifier
- feat(specman-cli): list constraints
- feat(specman-cli): read constraint content
- rev(specman-core): constraint-based unit test adapters (create test files)
- feat(specman-library): constraint based unit test adapters (rust)
- feat(specman-cli): constraint unit test creation command
- rev(specman-core): compliance tracking (constraints / complying tests)%
- rev(specman-cli): compliance tracking command details
- rev(specman-mcp): compliance tracking function / resources(?)
- feat(specman-library): compliance tracking implementation
