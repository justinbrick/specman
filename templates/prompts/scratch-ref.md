# Scratch Pad Prompt — Refactor

Target: {{target_path}}

Dependencies:

{{context}}

Steps:

1. {{branch_name}}
2. Call the MCP tool `create_artifact` with `kind = scratch_pad`, `scratch_kind = ref`, and `target = {{target_path}}`.

## User Input

- Provide the refactor guidance here. Keep this section at the bottom so user input stays isolated from the prompt structure.
